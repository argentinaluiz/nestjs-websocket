version: '3'

services:

  app:
    image: argentinaluiz/nestjs-websockets
    deploy:
      replicas: 3
      placement:
        constraints: [node.role==worker]
    build: 
      context: .
      dockerfile: Dockerfile.prod
    ports:
      - "3000:3000"