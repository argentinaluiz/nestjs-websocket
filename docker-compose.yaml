version: '3'

services:

  app:
    image: argentinaluiz/nestjs-websockets
    deploy:
      replicas: 1
      placement:
        constraints: [node.role==worker]
    build: 
      context: .
      dockerfile: Dockerfile.prod
    ports:
      - "3000:3000"